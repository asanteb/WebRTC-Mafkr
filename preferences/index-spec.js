require('../spec-helper')

describe('Preferences', function () {
  describe('video source', function () {
    it('allows camera source selection')

    context('when revisiting', function () {
      it('remembers previously selected camera source')
    })
  })
})
